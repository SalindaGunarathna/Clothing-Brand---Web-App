const USER_ROLES = Object.freeze({
  USER: 'USER',
  ADMIN: 'ADMIN'
});

const PRODUCT_CATEGORIES = Object.freeze({
  MEN: 'MEN',
  WOMEN: 'WOMEN',
  KIDS: 'KIDS'
});

const PRODUCT_SIZES = Object.freeze({
  S: 'S',
  M: 'M',
  L: 'L',
  XL: 'XL'
});

module.exports = { USER_ROLES, PRODUCT_CATEGORIES, PRODUCT_SIZES };
